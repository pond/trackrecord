<%= render(
      {
        :partial => 'shared/active_inactive_list',
        :locals  =>
        {
          :model          => 'customer',
          :active         => @active_customers,
          :inactive       => @inactive_customers,
          :columns        => @columns,
          :actions_method => 'customerhelp_actions'
        }
      }
    )
%>
      <h2>Find customers</h2>

      <% form_tag customers_path, :method => 'get' do %>
        <%= hidden_field_tag( 'sort',      params[ :sort      ] ) %>
        <%= hidden_field_tag( 'direction', params[ :direction ] ) %>
        <%= hidden_field_tag( 'entries',   params[ :entries   ] ) %>
        <% content_tag :label do %>
          Search customer titles and codes (TBD - created-at ranges):
          <%= text_field_tag :search, params[ :search ] %>
          <%= submit_tag( 'Go', { :name => 'search_go' } ) %>
<% if ( params[ :search ] ) -%>
          <%= submit_tag( 'Clear', { :name => 'search_cancel' } ) %>
<% end -%>
        <% end %>
      <% end %>